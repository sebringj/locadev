# Azure AI Search emulator (Qdrant-backed)

Serves the subset of Azure AI Search REST that `azure-search-documents` calls.

**Limitations:** partial OData `$filter`; no real semantic L2 reranker; hybrid is vector-dominant.
